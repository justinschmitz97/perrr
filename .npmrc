# Hoist all transitive deps to root node_modules so that test fixtures
# outside of any workspace package (fixtures/acceptance/*) can resolve
# React, RTL, radix, motion, etc. via Node's upward lookup.
# Without this, pnpm's isolated layout causes vite to fail with
# "Failed to resolve import 'react/jsx-dev-runtime'".
shamefully-hoist=true
