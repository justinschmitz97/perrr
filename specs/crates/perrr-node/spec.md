---
title: perrr-node
kind: crate
status: approved
related:
  - specs/overview/00-tdd.md
  - specs/decisions/0002-native-via-napi-rs.md
  - specs/milestones/m1-workspace-skeleton.md
tests:
  - crates/perrr-node/tests/hello.rs
  - packages/perrr/test/smoke.test.ts
last-reviewed: 2026-04-28
---

## Purpose
- Single napi-rs binding layer between Rust core and Node.js V8.
- All JS↔Rust traffic flows through this crate.
- No business logic; thin wrapper that delegates to downstream crates (`perrr-dom`, `perrr-style`, …) in future milestones.

## Contract
### MUST
- Crate type: `["cdylib", "rlib"]`.
- Expose `hello() -> "ok"` via `#[napi]`.
- All panics caught at N-API boundary; translated to `napi::Error` → `JsError`.
- Handle-based API: cross-boundary node references are `u32` `NodeId`; never pointers, refs, or `Box<T>`.
- Build via `@napi-rs/cli build`; artifact = single `.node` per `{os, arch, libc}` triple.
- TS types in `packages/perrr/index.d.ts` generated by napi-rs.

### MUST NOT
- Depend on any `perrr-*` crate at M1 (plumbing only).
- Introduce Rust deps beyond `napi`, `napi-derive`, `napi-build`.
- Hold mutable global state (state lives downstream; passed by `NodeId`).
- Expose raw Rust types to JS; use `#[napi(object)]` or string-keyed dicts at the boundary.

### Invariants
- A Rust panic never unwinds into V8 (`napi-derive` wraps entry points in `catch_unwind`).
- `hello()` is side-effect-free.
- N-API ABI version pinned per release; compat asserted in CI.

## Non-goals
- No DOM / style / layout / paint / scheduler / perf / thrash logic (downstream crates, M2+).
- No async ops at M1.
- No `Buffer` / `TypedArray` interop at M1.

## Design
- `src/lib.rs`: `#[napi] pub fn hello() -> String`.
- `build.rs`: `napi_build::setup()`.
- Future pattern (M2+): each downstream subsystem exposes a Rust struct; perrr-node wraps it with `#[napi]` and stores instance in env-scoped state.

## Target triples (v0.1)
| rust triple | npm target |
|---|---|
| x86_64-pc-windows-msvc | win32-x64-msvc |
| x86_64-apple-darwin | darwin-x64 |
| aarch64-apple-darwin | darwin-arm64 |
| x86_64-unknown-linux-gnu | linux-x64-gnu |
| aarch64-unknown-linux-gnu | linux-arm64-gnu |
| x86_64-unknown-linux-musl | linux-x64-musl |

## Tests
- `crates/perrr-node/tests/hello.rs` — `#[test] assert_eq!(perrr_node::hello(), "ok");`
- `packages/perrr/test/smoke.test.ts` — `expect(hello()).toBe("ok");`

## Open
- _(none)_

## Changelog
- 2026-04-28: initial.
- 2026-04-28: pinned `@napi-rs/cli` to `^3.0.0` (v2 CLI lacks `--manifest-path`; v3 required for split crate/package layout).
